# AI 协作指南 —— 如何与 lan 高效协作开发

> 写给接手本项目的 AI 助手。遵守这些规则能让协作顺畅 10 倍。

---

## 一、语言铁律

| 场景 | 语言 |
|------|------|
| 回复用户、解释代码、汇报进度 | **中文** |
| 操作描述（"让我读取文件"、"修复完成"） | **中文** |
| 制定计划（Plan、TodoWrite）、写 devlog | **中文** |
| 代码标识符（变量名、函数名、类名） | 英文 |
| Git 提交信息 | **中文** |
| 用户可见界面文本 | 中文 |

**一句话：除了代码本身，全部用中文。**

---

## 二、首次接手项目 — 必读文件

新 AI 进入项目后，**第一步**按顺序读这三个文件：

1. **`README.md`** — 项目是什么、怎么启动、文件夹结构
2. **`CLAUDE.md`** — 所有代码规范、Git 规范、重大改动记录、Bug 修复规范
3. **`devlog.md`** — 完整开发历史，了解每个阶段的完成情况和历史 Bug 的根因

读完这三个文件后，快速扫一眼 `backend/` 和 `frontend/src/` 的目录结构即可开始工作。**不需要读 CSV 数据文件。**

---

## 三、Bug 修复 — 最重要的纪律

每次修完 Bug 后**必须立即**做两件事，不能等用户提醒：

### 3.1 更新 devlog.md

追加格式如下：

```markdown
## [日期] - Bug 修复

### 前端
- 问题：（用户看到的现象）
- 原因：（根因分析）
- 修改：（具体改了什么）

### 后端
- 问题：……
- 原因：……
- 修改：……
```

**规则**：
- 按模块分类（前端 / 后端 / 数据预处理 / 其他），无 bug 的模块不写
- 每个 bug 必须有**问题、原因、修改**三个要素
- 在 TodoWrite 中把"更新 devlog"和"git 提交"作为独立任务项

### 3.2 Git 提交

```bash
git add <源码文件> devlog.md    # 源码和 devlog 合并为一次提交，不要分开两次
git commit -m "fix: <简述>"
git push
```

- 提交信息格式：`<类型>: <简述>`
- 类型：`feat` / `fix` / `refactor` / `docs` / `test`
- 一次 commit 同时包含源码和 devlog，**不要分开两次提交**

---

## 四、架构级变更 — 额外记录

如果改动满足以下**任一条件**，就是架构级变更，必须在 `CLAUDE.md` 第九节的"近期重大改动"表格中追加一行：

- 改变了前后端数据协议（API 参数/响应结构）
- 新增或删除了全局状态管理机制
- 改变了多个 UI 之间的通信或同步方式
- 新增了持久化数据文件
- 改变了核心数据流处理方式

追加格式：

```markdown
| 日期 | 变更 | 涉及文件 |
|------|------|----------|
| YYYY-MM-DD | 一句话描述变更了什么 | file1, file2 |
```

> 判断标准：如果一个改动，新接手的 AI 不知道就会写出有 bug 的代码——那就是架构级变更。

---

## 五、编程前 — 先规划

- **非平凡任务**（多文件修改、新功能、架构决策）必须先 `EnterPlanMode`，用中文写方案，让用户确认后再写代码
- **简单任务**（单行修复、明确的小改动）可以直接动手
- 不确定时宁可用 Plan mode，不要盲目写代码

---

## 六、关键代码规范速查

摘自 `CLAUDE.md`：

### Python 后端
- 遵循 PEP 8，函数不超过 50 行
- 路由层（`routes/`）只做参数校验和响应格式化，**不写业务逻辑**
- 业务逻辑全部放在 `services/`
- 可配置常量集中在 `backend/utils/config.py`
- API 响应格式：`{"status":"ok","data":{...}}` 或 `{"status":"error","message":"..."}`
- CSV 编码 `utf-8-sig`

### Vue 3 前端
- 使用 Composition API（`<script setup>`）
- 组件名 PascalCase
- 单文件组件结构：`<template>` → `<script setup>` → `<style scoped>`
- ECharts 生命周期：`onMounted` init → `onUnmounted` dispose
- 颜色常量统一在 `shared/constants.js` 管理

### 路径规范
- 所有路径使用正斜杠 `/`（Windows 兼容）
- 数据根目录 `data/`，日期子目录 `YYYYMMDD`

---

## 七、该做和不该做

### 该做

- 修改代码后回顾已完成的旧模块，确保不引入回归
- 运行测试验证（如有）
- 编辑现有文件优先于新建文件
- 用 Edit 工具做小改动，Write 工具做完整重写
- 先检查文件是否存在再写

### 不该做

- **不要**主动创建 `README.md` 或 `*.md` 文档文件（除非用户明确要求）
- **不要**写多行 docstring 或大段注释（一行简短注释即可）
- **不要**为"未来可能的需求"写代码
- **不要**在代码里写 `console.log`
- **不要**跳过 git hook（`--no-verify`）
- **不要** amend 已发布的 commit
- **不要**对 `main` 分支 force push
- **不要**把 `.env`、`credentials.json` 等敏感文件提交到 git

---

## 八、Memory 系统使用指南

用户启用了持久记忆系统，位于 `C:\Users\Admin\.claude\projects\d--Code-SLTD\memory\`。

### 什么时候存 memory

- 用户纠正了你的做法（"不要这样"、"别用 X"）→ 存为 feedback
- 用户确认了非显而易见的做法（"对，就这样"）→ 存为 feedback
- 了解到用户角色、偏好、知识背景 → 存为 user
- 了解到项目背景、目标、约束、时间线 → 存为 project
- 了解到外部资源位置（问题追踪、监控面板）→ 存为 reference

### 什么时候不存 memory

- 代码模式/架构/文件路径 — 读代码可得
- Git 历史 — `git log` 是权威源
- Bug 修复方案 — 已在代码和 devlog 中
- 已写在 CLAUDE.md 的内容 — 不必重复

### 存 memory 的格式

两步：先写独立的 `.md` 文件（含 frontmatter），再在 `MEMORY.md` 中加一行索引。

---

## 九、交流风格

- 回应简洁，不要每轮都总结"做了什么"
- 关键节点才汇报（找到问题了、方向变了、遇到阻塞了）
- 不确定的事情先问，不要猜
- 危险操作（`rm -rf`、`git reset --hard`、`force push`）必须先确认
- 涉及 `communication*.md` 文档时，所有需要用户回复的区域每行以 `>` 开头
