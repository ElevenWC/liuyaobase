# 文件规划：backend/schemas/tag.py

## 1. 基本信息
- **所属模块**：schemas/
- **优先级**：P1
- **依赖文件**：`backend/models/tag.py`
- **被依赖文件**：`backend/api/routers/tags.py`

## 2. 核心职责
标签请求/响应 Pydantic 模型。

## 3. 对外接口
```python
class TagCreateRequest(SQLModel):     # name: str, parent_id: int | None
class TagUpdateRequest(SQLModel):     # name: str
class TagTreeNode(SQLModel):          # 树节点（含 children: list[TagTreeNode]）
```

## 4. 内部逻辑要点
- TagTreeNode 递归引用自身实现树形结构
- 使用 `from __future__ import annotations` 或 `model_rebuild()` 解决前向引用

## 5. 测试要点
- 请求体校验
- 树形响应 JSON 序列化正常

## 6. 潜在陷阱与风险
- [递归序列化] Pydantic 递归模型需 `from __future__ import annotations` (Python 3.10+) 或在类定义后调用 `TagTreeNode.model_rebuild()`。
