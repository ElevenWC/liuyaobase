# 文件规划：backend/crud/guaci.py

## 1. 基本信息
- **所属模块**：crud/
- **优先级**：P1
- **依赖文件**：`backend/models/guaci.py`
- **被依赖文件**：`backend/api/routers/jiegua.py`、`frontend/src/components/shared/GuaCiFloat.vue`

## 2. 核心职责
卦爻辞表只读查询——按卦代码返回卦辞/彖传/象传/爻辞/文言/用九用六。

## 3. 对外接口
```python
def get_by_code(session, code: str) -> GuaCi | None: ...
```

## 4. 内部逻辑要点
- 纯只读，64 条在 v0.0 已从 zhouyiData.json 填充
- yao_ci 是 JSON 字段：`{"初爻": "...", "二爻": "...", ...}`
- wenyan 仅乾坤二卦有值，其他卦为 NULL
- yong 仅乾坤二卦有值（用九/用六）

## 5. 测试要点
- 乾卦(111111)→卦辞/彖传/象传/文言/用九 全有值
- 普通卦→wenyan 和 yong 为 NULL，不影响前端显示

## 6. 潜在陷阱与风险
- [文言分隔符] 文言中 `|` 表示换行，存入时原样保留，前端替换为 `<br>`。不要在 CRUD 层做替换。
