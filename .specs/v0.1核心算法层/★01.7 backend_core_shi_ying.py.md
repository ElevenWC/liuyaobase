# 文件规划：backend/core/shi_ying.py 🟡

> 🟡 重要：世应定位错误 → 检索"世爻为妻财"等条件全部失准。

## 1. 基本信息
- **所属模块**：core/
- **优先级**：P0（必须）
- **依赖文件**：`backend/core/enums.py`
- **被依赖文件**：`backend/services/precalculate_service.py`、`backend/core/fushen_zengshan.py`

## 2. 核心职责
B4 世应定位：根据卦的宫位类型确定世爻和应爻的位置。世应相隔两爻。本卦和之卦使用相同的定位规则。

## 3. 对外接口

```python
def get_shi_ying(palace_type: str) -> tuple[int, int]:
    """输入宫位类型，返回(世爻位置, 应爻位置)
    例: get_shi_ying("归魂") → (3, 6)  # 世在三爻，应在上爻
    """

def get_shi_ying_labels(palace_type: str) -> list[str]:
    """返回6爻的世应标签列表
    例: get_shi_ying_labels("一世") → ["世","","","应","",""]
    """
```

## 4. 内部逻辑要点

**世应位置表**：

| 宫位 | 世爻 | 应爻 | 校验（世应相隔两爻） |
|------|:--:|:--:|------|
| 本宫 | 6(上爻) | 3(三爻) | 间隔4,5 ✓ |
| 一世 | 1(初爻) | 4(四爻) | 间隔2,3 ✓ |
| 二世 | 2 | 5 | 间隔3,4 ✓ |
| 三世 | 3 | 6(上爻) | 间隔4,5 ✓ |
| 四世 | 4 | 1(初爻) | 间隔2,3 ✓ |
| 五世 | 5 | 2 | 间隔3,4 ✓ |
| 游魂 | 4 | 1(初爻) | 间隔2,3 ✓ |
| 归魂 | 3 | 6(上爻) | 间隔4,5 ✓ |

## 5. 输入输出示例
```python
get_shi_ying("一世")    # → (1, 4)
get_shi_ying("归魂")    # → (3, 6)
get_shi_ying_labels("一世")  # → ["世", "", "", "应", "", ""]
```

## 6. 测试要点
- 8种宫位类型全部覆盖
- 验证世应相隔两爻：`abs(shi - ying)` 一定是 3（隔两爻）
- 之卦世应：之卦用自己的宫位类型查同一张表

### 用户测试 👤
```bash
python -c "
from backend.core.shi_ying import get_shi_ying
for t in ['本宫','一世','二世','三世','四世','五世','游魂','归魂']:
    s, y = get_shi_ying(t)
    print(f'{t}: 世={s}, 应={y}, 相隔={abs(s-y)}')
"
```
预期：所有宫位相隔均为 3（世应隔两爻）

## 7. 潜在陷阱与风险
- [之卦世应用错规则] 之卦世应用之卦自身的宫位类型计算，不是本卦的宫位类型。且之卦世应信息所有6爻共享。
- [游魂和四世相同] 游魂(4,1) 和 四世(4,1) 世应位置相同——这是正确的，两者宫位类型不同但世应位置恰好一致。
