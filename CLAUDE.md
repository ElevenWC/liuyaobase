# CLAUDE.md — liuyaobase 项目 AI 协作指南

---

## 一、项目概况

- **名称**：liuyaobase（六爻数据库系统）
- **技术栈**：Python FastAPI + SQLModel + MySQL + Vue.js 3 + AKShare + lunar-python
- **当前阶段**：**详细规划阶段已完成**，即将进入编码实现阶段

---

## 二、当前阶段：编码实现阶段

> 详细规划阶段已完成（63 个规划文档在 `.specs/` 下）。当前进入编码实现阶段。

### 2.1 规划文档 = 实现计划

每个文件的规划文档（`.specs/<里程碑>/`）已包含编码所需的全部信息：
- **§3 对外接口** → 函数签名直接指导编码
- **§4 内部逻辑要点** → 算法步骤和数据处理流程
- **§6 测试要点** → AI 自测 + 用户测试命令
- **§7 潜在陷阱与风险** → 自审时逐项对照检查

**AI 不需要在 Issue 评论里再写实现计划——直接读规划文档即可开始编码。**

### 2.2 编码流程速查

```
用户选 Issue（对应一个 .specs/ 规划文档）
      │
      ▼
AI 读规划文档 + 设计文档
      │
      ├── 有疑问？→ Issue 评论提问 → 用户确认
      │
      ▼
AI 切分支 feat/issue-N，写代码
      │
      ▼
AI 自审两遍（必须逐行审查，不可跳过）：
  第一遍 —— 对照规划文档 §7 陷阱 + §9 审查清单逐项检查：
    · SQL 注入：所有用户值走参数化查询，字段名白名单校验
    · 空值/NULL：LEFT JOIN 场景下 IS NULL 处理
    · 参数绑定：占位符 :param 与 params[key] = val ——配对
    · 边界值：空列表、空字符串、0、负数
    · 字段一致性：前后端字段名、数据库列名
  第二遍 —— 模拟运行时路径逐行跟踪：
    · 重读每个函数从入口到出口，确认无未使用变量
    · 确认条件分支覆盖（if/elif/else 均有处理）
    · 确认错误路径有明确的异常或兜底值（而非静默吞错）
    · 对照参考实现或现有 C1/C2 代码验证风格一致性
      │
      ▼
AI 跑规划文档 §6 的测试命令 → 全部通过
      │
      ▼
AI commit + push（不创建 PR）
      │
      ▼
用户跑 §6 的用户测试命令
      │
      ├── 有问题 → 同分支修改 → push → 用户再测
      │
      ▼
用户验收通过 → AI 创建 PR → squash merge → 清理分支
      │
      ▼
AI 逐项检查（squash merge 后必做）：
  ├── Issue 是否已关闭？
  ├── 本地分支是否已删除？
  ├── 远程分支是否已删除？（git fetch --prune）
  └── git status 确认工作区干净
```

### 2.3 文件标记与审查强度

| 规划文档标记 | 编码阶段要求 |
|------------|------------|
| `★★` | 用户**逐条审查代码逻辑**。AI 写完后逐行解释关键逻辑 |
| `★` | 用户重点审查关键函数和边界处理 |
| 无标记 | 正常流程——用户跑测试命令，通过即可 |

### 2.4 Issue 模板（精简版）

```markdown
> 📖 规划文档：`.specs/v0.X.../文件名.md`
> 📖 设计文档：`.AIDiscuss/.X...md`

## 验收标准
- [ ] <从规划文档 §6 提取>
- [ ] 用户测试命令全部通过
```

### 2.5 并行开发策略

规划文档中标记了依赖关系：
- **📦 可并行**：互不依赖的单元可以同时切不同分支开发
- **🔗 需联调**：需要先后完成，后完成的 Issue 依赖先完成的
- **先做被依赖的**：规划文档 §1 中"被依赖文件"越多的越先做

### 2.6 Git 规范

- **分支命名**：`feat/issue-N`（功能）、`fix/issue-N`（修复）、`chore/xxx`（杂务）
- **禁止**直接推 main、force push、跳过 hooks、commit 敏感文件
- **Commit 格式**：`feat: <中文简述>`，PR 描述写 `Closes #N`
- **合并方式**：squash merge，合并后删除分支

### 2.7 代码审查清单（两遍制，每次 commit 前必须完成）

**第一遍 — 对照规划文档自查：**

| 层级 | 检查项 |
|------|--------|
| 通用 | 硬编码是否可改为配置？跨模块修改影响评估？错误信息中文友好？ |
| 后端 | 路由层不写业务逻辑、API 响应格式统一、字段变更搜索所有引用处 |
| 前端 | v-model 绑定、ref/computed 定义、异步错误处理、定时器清除 |
| 数据流 | 前后端字段名一致、保存后读取可恢复、区分 static_* vs guali_* |
| 安全 | 用户输入全部走参数化查询、字段名白名单校验、无字符串拼接 SQL |

**第二遍 — 模拟运行时逐行跟踪：**

| 维度 | 检查项 |
|------|--------|
| 完整性 | 每个 if/elif/else 分支均有处理、无未使用变量、无死代码 |
| 边界 | 空列表、空字符串、NULL、0、负数均不导致崩溃 |
| 错误 | 异常路径有明确的 ValueError/raise 或兜底逻辑，不静默吞错 |
| 一致性 | 与现有 C1/C2 代码风格、变量命名、CSS 变量体系一致 |

### 2.8 遗留占位追踪

当某个功能的实现因依赖后面的里程碑而暂时留空时，必须做到以下三点，防止遗忘：

**（1）代码中用 `raise NotImplementedError` 替代空函数体或静默跳过：**

```python
def seed_static_data(session: Session):
    """填充 static_* 三表数据，v0.2 预计算的前置条件"""
    raise NotImplementedError("v0.2 编码前需实现：填充 static_gua_yao_info(384条) + static_fushen_zengshan(~64条) + static_fushen_yimao(384条)")
```

**（2）依赖方 Issue 正文中写明前置占位：**

在 Issue 描述中增加"前置依赖"小节，列出需要先补上的占位代码（文件路径 + 函数名）。

**（3）每个里程碑收尾时检查遗留清单，写入下一个里程碑描述：**

```
# AI 收尾命令（搜索所有占位标记）
grep -rn "NotImplementedError\|TODO\|FIXME\|placeholder\|暂\|留空\|v0\." backend/ --include="*.py"
```
将结果汇总写入下一里程碑描述的"前置待办"区域。

---

## 四、项目文件结构

```
liuyaobase/
├── CLAUDE.md              ← 本文件
├── .AIDiscuss/            ← 设计文档（权威参考，13个）
├── .specs/                ← 开发规划文档（63个，7个里程碑）
│   ├── READMEsp.md        ← 开发架构总览
│   ├── v0.0项目骨架_数据库/
│   ├── v0.1核心算法层/
│   ├── v0.2数据导入_预计算服务/
│   ├── v0.3C1卦例显示/
│   ├── v0.4C3复杂检索/
│   ├── v0.5C2解卦模块/
│   └── v0.6C4股票关联/
├── .user/                 ← 用户个人参考（AI不主动读取）
└── .gitignore
```

---

## 五、关键参考

| 想看什么 | 文件 |
|---------|------|
| 开发架构总览、文件清单、里程碑 | .specs/READMEsp.md |
| 数据库表结构 | .AIDiscuss/.A1数据库设计.md |
| 后端架构和 API | .AIDiscuss/.A2后端架构.md |
| 六爻算法规则 | .AIDiscuss/.B核心算法.md |
| 各模块功能设计 | .AIDiscuss/.C1~.C4*.md |
| 项目全局状态 | .AIDiscuss/.Z项目总结.md |
| 开发流程详细规范 | .user/AI协作工作流程指南(项目开发阶段).md |

---

## 六、开发流程速查

### 详细规划阶段（已完成 ✅）

```
用户指定里程碑 → AI 生成规划文档 → 用户审查 → 存入 .specs/
```

### 编码实现阶段（当前 ←）

```
用户选 Issue → AI 读 .specs/ 规划文档 → 切分支写代码
    → 自审两遍（对照规划文档 §7 陷阱）→ 跑 §6 测试命令
    → commit + push + 创建 PR → 用户跑测试命令
    → 验收通过 → squash merge → 清理分支
```

### 模块联调阶段（未来）

```
里程碑全部 Issue 合并 → 跑全量单元测试 → 用户端到端操作验收
    → 发现问题 → 创建 Bug Issue → fix/issue-N 修复
```

---

*CLAUDE.md v2.1*
*创建时间：2026-05-09*
*更新时间：2026-05-23*
*当前阶段：编码实现阶段*
*更新内容：新增 §2.8 遗留占位追踪——代码中用 NotImplementedError 标注、Issue 中写明前置依赖、里程碑收尾时汇总清单到下一里程碑*
